// Vendored from signalk-client-angular ^2.1.0 (Apache 2.0, AdrianP).
// Pure ESM, named exports, tree-shakable.
//
// Only SignalKClient is re-exported; everything else stays internal.

export { SignalKClient } from './signalk-client.service';
